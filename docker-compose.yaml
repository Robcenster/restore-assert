services:
    db:
      image: postgres:17-alpine
      ports: 
        - 5313:5432
      environment:
        POSTGRES_PASSWORD: postgres
        POSTGRES_USER: postgres
        POSTGRES_DBNAME: postgres
        POSTGRES_PORT: 8080